{
  "name": "@adapter/indexer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Event-sourced indexer, API and demo UI for Adapter8004 v0.0.17 (UBI identities, counterfactual claims, wallet UBIs, attestations)",
  "scripts": {
    "test": "vitest run",
    "demo": "tsx src/run-demo.ts",
    "serve": "tsx src/server.ts"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
